{
  "name": "@ai-support/server",
  "version": "0.1.0",
  "description": "Widget backend — Cloudflare Worker that mints qlaud per-end-user keys and proxies streaming chat.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.86.0"
  }
}
